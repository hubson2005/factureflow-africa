import AIRecoveryPanel from "../panels/AIRecoveryPanel";

// Page de recouvrement — accessible via /recovery, partage DashboardLayout
// (sidebar + bottom nav) comme Clients, Invoices, Payments...
export default function Recovery() {
  return <AIRecoveryPanel />;
}